# Mobile API configuration (redacted)

Runtime endpoints and any client identifiers must be supplied by deployment environment variables. No API credentials belong in source control. Rotate any credentials that were previously committed.
