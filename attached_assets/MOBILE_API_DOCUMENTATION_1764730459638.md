# Mobile API documentation (sanitized)

Endpoint documentation may be maintained here, but credentials and private deployment identifiers are intentionally omitted. Configure runtime values through deployment environment variables and rotate any previously committed keys.
